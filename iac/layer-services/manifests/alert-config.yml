global:
  resolve_timeout: 5m
route:
  group_by: ['cluster']
  receiver: 'slack-forwarder'
  routes:
  - match:
      severity: slack
    receiver: slack_general
receivers:
- name: slack_general
  slack_configs:
  - api_url: $WEBHOOK_URL
    channel: '#demo'